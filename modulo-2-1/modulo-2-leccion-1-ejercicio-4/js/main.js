'use strict';

const titleElement = document.querySelector('.title');
const adalaberInma = document.querySelector('.adalaber__inma');
const adalaberSara = document.querySelector('.adalaber__sara');

// titleElement.innerHTML = titleElement.innerHTML + ' ' + adalaberInma.innerHTML;

titleElement.innerHTML = titleElement.innerHTML + ' ' + adalaberSara.innerHTML;