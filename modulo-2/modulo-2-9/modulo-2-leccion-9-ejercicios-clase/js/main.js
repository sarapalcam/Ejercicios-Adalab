"user strict";

/* //Info de métodos y propiedades de arrays en MDN

const ul = document.querySelector(".js_ul");

const list = ["Alejandra", "Alicia", "Ana", "Angeles", "Bárbara", "Claudia"];

console.log(list);

//Tamaño de mi lista
console.log(list.length);

//Dar la vuelta a mi lista
list.reverse();
console.log(list);

//Posición de un elemento de la lista
const position = list.indexOf("Alicia");
console.log(position);

//Concatenar
const list2 = ["Ana", "Paula", "Sara"];
const result = list.concat(list2);
console.log(result);

//Añadir elementos al listado
list.push("Begoña", "Isabel", "Rosa");
console.log(list);
result.push("Begoña", "Isabel", "Rosa");
console.log(result);

//Añadir elementos al inicio del listado
list.unshift("Elena");
console.log(list);

//Ordenar mi lista alfabéticamente
list.sort();
console.log(list);
list.sort().reverse();
console.log(list);

//Slice(posiciónInicial, posiciónFinal): extrae de posiciónInicial a posiciónFinal -1
const slice = list.slice(1, 3);
console.log(slice); // Array con los elementos que he extraído
console.log(list); // Array original, no hay cambios

//Splice(posiciónInicial, cantidadElementos): Este es el ejemplo sin sustitución de elementos
const splice = list.splice(1, 3);
console.log(splice); // Array con los elementos que he extraído
console.log(list); // Array original sin los elementos eliminados en el splice

//Splice(posiciónInicial, cantidadElementos): Este es el ejemplo cons sustitución de elementos
const splice2 = list.splice(1, 3, "Mari Carmen", "Amparo");
console.log(splice2); // Array con los elementos que he extraído
console.log(list); // Array original sin los elementos eliminados en el splice

//Pintamos todos los elementos de la lista con un bucle for
for (let i = 0; i < list.length; i++) {
  const name = list[i];
  ul.innerHTML += `<li>${name}</li>`;
}
 */
